<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
             xmlns:behaviors="clr-namespace:Kari.LoadingAssist.UI.Behaviors"
             xmlns:forms="clr-namespace:Lottie.Forms;assembly=Lottie.Forms" 
             x:Class="Kari.LoadingAssist.UI.AnimatedLoading.AnimatedLoadingV">
    <ContentView.Behaviors>
        <behaviors:EventToCommandBehavior  EventName="Appearing" Command="{Binding PageAppearingCommand}" />
        <behaviors:EventToCommandBehavior EventName="Disappearing" Command="{Binding PageDisappearingCommand}"/>
    </ContentView.Behaviors>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <forms:AnimationView 
                x:Name="animationView" 
                Grid.Row="0"
                Animation="{Binding AnimationSource}" 
                RepeatMode="Restart"
                IsAnimating="True"
                IsVisible="True"
                AutoPlay="True"
                VerticalOptions="FillAndExpand" 
                HorizontalOptions="FillAndExpand" />
        <Label x:Name="labLoadingText"
                   Text="{Binding LoadingTextValue}"
                   Grid.Row="1"
                   HorizontalTextAlignment="Center"/>
    </Grid>
</ContentView>